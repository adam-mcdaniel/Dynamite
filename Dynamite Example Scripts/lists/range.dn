(
    (call for "x" (call range 1 3)
        (function(
            (call print "Random number between 1 and 10: ")
            (call print (call random.randint 1 10))
            (call print endl)            
        ))
    )

    (call input)
)